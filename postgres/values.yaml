image:
  tag: "17.6.0-debian-12-r4"

auth:
  username: pguser
  database: mydb
  existingSecret: postgres-secret
  secretKeys:
    userPasswordKey: password

primary:
  persistence:
    enabled: false